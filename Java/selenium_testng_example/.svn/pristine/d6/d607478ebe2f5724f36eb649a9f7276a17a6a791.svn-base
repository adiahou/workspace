package dxy.vs.testcase.system;

import org.openqa.selenium.support.PageFactory;
import org.testng.annotations.Test;

import dxy.vs.base.ReusableFunctions;
import dxy.vs.base.SetUp;
import dxy.vs.pages.System_ManageSite;
import dxy.vs.pages.System_SiteSourcePage;

public class H05_siteSource extends SetUp {
	
	//Initialize Pages
	ReusableFunctions reusableFunctions;
	System_ManageSite system_ManageSite;
	System_SiteSourcePage siteSourcePage;
	
	@Test
	public void verifySiteSource() throws Exception{
		
		reusableFunctions = PageFactory.initElements(threadDriver.get(), ReusableFunctions.class);
		system_ManageSite = PageFactory.initElements(threadDriver.get(), System_ManageSite.class);
		siteSourcePage = PageFactory.initElements(threadDriver.get(), System_SiteSourcePage.class);
		
		reusableFunctions.login(0);
		
		system_ManageSite.openSiteManagePage();
		
		siteSourcePage.openSiteSourcePage();
		
		siteSourcePage.clearOldDatas();
		
		siteSourcePage.newSource();
		
		siteSourcePage.editSource();
		
		siteSourcePage.deleteSourceData("AutoTest_link_update");
	}
}
